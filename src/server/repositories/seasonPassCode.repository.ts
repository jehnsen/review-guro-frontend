/**
 * Season Pass Code Repository
 * Data access layer for SeasonPassCode entity
 *
 * TODO: This repository is temporarily disabled because the SeasonPassCode model
 * doesn't exist in the Prisma schema. Add the model to prisma/schema.prisma to enable this.
 */

// Placeholder export to prevent import errors
export const seasonPassCodeRepository = {} as any;
